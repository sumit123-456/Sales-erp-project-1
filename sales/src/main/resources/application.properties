spring.application.name=sales

# MySQL Configuration
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=KpKFBtklIGZRsqnvcgYBGzdRMXzRRSIc
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.datasource.hikari.initialization-fail-timeout=0
spring.jpa.open-in-view=false

#Server port (Railway sets PORT automatically)
server.port=8080
server.address=0.0.0.0

# HikariCP (Connection Pool)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=60000

# JWT Configuration
jwt.secret=eyJhbGciOiJIUzI1NiJ9.eyJyb2xlIjoiQURNSU4iLCJzdWIiOiJzdW1pdCIsImlhdCI6MTc0NDM1NzE2NCwiZXhwIjoxNzQ0NDQzNTY0fQ.GcFxskE2lw9FoI1QvpvHysj7Kh6mGnuIebKGkGX61tY

#24 hours in milliseconds
jwt.expiration=86400000 

#7days in milliseconds
jwt.refresh.expiration=604800000 

# Security
spring.security.oauth2.resourceserver.jwt.secret-key=${jwt.secret}

# Logging for debugging JWT
logging.level.com.sales.sales.security=DEBUG

# Disable warning
spring.jpa.open-in-view=false

# Expose actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoints.web.base-path=/actuator
